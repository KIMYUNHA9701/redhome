<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.ssangyong.redhome.dao.ProductMapper">

    <select id="selectAllProduct" resultType="com.ssangyong.redhome.bean.Product">
        SELECT * FROM PRODUCT
    </select>

    <insert id="insertProduct" parameterType="com.ssangyong.redhome.bean.Product">
        INSERT INTO PRODUCT(PRODUCT_NUM, PRODUCT_NAME, PRODUCT_IMG, PRODUCT_PRICE, PRODUCT_QUANTITY, PRODUCT_QUANTITY_SOLD, PRODUCT_TYPE_NUM, PRODUCT_CATEGORY_NUM)
        VALUES(#{product_num}, #{product_name}, #{product_img}, #{product_price}, #{product_quantity}, #{product_quantity_sold}, #{product_type_num}, #{product_category_num})
    </insert>

    <delete id="deleteProduct" parameterType="Integer">
        DELETE FROM PRODUCT WHERE PRODUCT_NUM = #{VALUE}
    </delete>
</mapper>